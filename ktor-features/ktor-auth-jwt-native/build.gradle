plugins {
    id 'org.jetbrains.kotlin.plugin.serialization'
}

description = ''

kotlin.sourceSets {
	jvmMain.dependencies {
        api project(':ktor-features:ktor-auth')
        api project(':ktor-jwt')
    }
    jvmTest.dependencies {
        api 'com.nhaarman:mockito-kotlin:1.6.0'
    }
}

